---
defaults:
  ssh_username:
  openstack_auth_url:
  username:
  password:
  tenant_name:
  keypair_name:
  private_key_path:
instance_defaults:
  flavor:
  floating_ip_pool:
  networks:
    - example_network
  security_groups:
    - example_security_group
  box: 'dummy'
instances:
  example1:
    primary: false
    autoup: true
    image: example_image
  testinstance2:
    primary: true
    autoup: true
    image: example_image_1
