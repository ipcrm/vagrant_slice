---
defaults:
  ssh_username:
  openstack_auth_url:
  username:
  password:
  tenant_name:
  keypair_name:
  private_key_path:
instance_defaults:
  flavor:
  floating_ip_pool:
  networks:
    - example_network
  security_groups:
    - default
  box: 'dummy'
  sync_method: none
instances:
  testinstance1:
    primary: false
    autoup: true
    image: example
  testinstance2:
    primary: true
    autoup: true
    image: example1
    ssh_username: example
    flavor: example
